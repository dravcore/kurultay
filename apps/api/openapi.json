{
  "openapi": "3.0.0",
  "paths": {
    "/health": {
      "get": {
        "description": "Unauthenticated and unthrottled. Static and dependency-free on purpose: a liveness probe that touched Postgres would have an orchestrator restart a healthy API because a dependency blipped, and a restart cannot heal the dependency.",
        "operationId": "HealthController_check",
        "parameters": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LivenessSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Liveness probe",
        "tags": ["Instance"],
        "security": []
      }
    },
    "/health/ready": {
      "get": {
        "description": "Unauthenticated and unthrottled. Probes Postgres, and Redis when the deployment configures it. **Both outcomes return the same document** — this is the one endpoint whose failure body is not the error envelope, because the caller is a probe reading `checks` to learn *which* dependency is down.",
        "operationId": "HealthController_ready",
        "parameters": [],
        "responses": {
          "200": {
            "description": "Ready to serve traffic.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "503": {
            "description": "At least one dependency is `down`. The body is this same probe document, never the error envelope.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        },
        "summary": "Readiness probe",
        "tags": ["Instance"],
        "security": []
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/attachments": {
      "get": {
        "description": "Newest first. Not paginated — a task holds few enough of these to return all.",
        "operationId": "AttachmentController_list",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/AttachmentSchema"
                  }
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "List a task's attachments",
        "tags": ["Attachments"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "description": "One endpoint, two request shapes, chosen by `kind` in the body and **never** inferred\nfrom whether a file part arrived:\n\n- `kind: \"FILE\"` — `multipart/form-data` with one part named `file`. The media type is\n  read from the **magic bytes**; the declared `Content-Type` and the filename extension\n  are not evidence and are not consulted. `text/html` and `image/svg+xml` are refused by\n  name. Plain text has no magic number and comes in through a narrow fallback: the\n  declared type must be exactly `text/plain` or `text/csv`, the bytes must decode as\n  UTF-8, contain no `NUL`, and not begin with `<`.\n- `kind: \"LINK\"` — `application/json` with a `url`. **The server stores it, returns it,\n  and never requests it** — no preview, no favicon, no unfurl, no health check. Only\n  `http:` and `https:` are accepted; anything else is `400`.\n\nA request carrying neither is `400` naming what is missing, rather than a guess.",
        "operationId": "AttachmentController_create",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "oneOf": [
                  {
                    "title": "FILE (multipart/form-data)",
                    "type": "object",
                    "required": ["kind", "file"],
                    "properties": {
                      "kind": {
                        "type": "string",
                        "enum": ["FILE"]
                      },
                      "file": {
                        "type": "string",
                        "format": "binary",
                        "description": "The bytes. Over `ATTACHMENT_MAX_BYTES` is `413`."
                      },
                      "filename": {
                        "type": "string",
                        "maxLength": 255,
                        "description": "Overrides the part's own filename. Never used as a path segment."
                      }
                    }
                  },
                  {
                    "title": "LINK (application/json)",
                    "allOf": [
                      {
                        "$ref": "#/components/schemas/CreateAttachmentDto"
                      }
                    ]
                  }
                ]
              }
            },
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "title": "FILE (multipart/form-data)",
                    "type": "object",
                    "required": ["kind", "file"],
                    "properties": {
                      "kind": {
                        "type": "string",
                        "enum": ["FILE"]
                      },
                      "file": {
                        "type": "string",
                        "format": "binary",
                        "description": "The bytes. Over `ATTACHMENT_MAX_BYTES` is `413`."
                      },
                      "filename": {
                        "type": "string",
                        "maxLength": 255,
                        "description": "Overrides the part's own filename. Never used as a path segment."
                      }
                    }
                  },
                  {
                    "title": "LINK (application/json)",
                    "allOf": [
                      {
                        "$ref": "#/components/schemas/CreateAttachmentDto"
                      }
                    ]
                  }
                ]
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AttachmentSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "413": {
            "description": "Two distinct ceilings answer with this status, told apart by the envelope's `error` field. `\"Payload Too Large\"`: the **file part** is over `ATTACHMENT_MAX_BYTES` (default `26214400` — 25 MiB), which is a disk ceiling and is unrelated to `REQUEST_BODY_MAX_BYTES`: multipart bodies are read by multer, which the JSON body limit never sees. `\"Attachment Quota Exceeded\"`: the file fits on its own but would push the workspace's or the instance's stored FILE bytes past its quota (`ATTACHMENT_WORKSPACE_QUOTA_BYTES`, default 2 GiB, or `ATTACHMENT_INSTANCE_QUOTA_BYTES`, default 20 GiB; `0` lifts one; ADR 0027). A reverse proxy in front of this API caps the whole request body separately and higher, and answers `413` with something that is not JSON at all — the response body is what tells the layers apart.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "415": {
            "description": "The file's magic bytes are not on the allowlist, or a `text/plain`/`text/csv` upload failed one of the four fallback conditions.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Two budgets answer with this status, told apart by the envelope's `error` field. `\"Too Many Requests\"`: more than 20 requests from this IP in the current minute, like any other throttled route. `\"Upload Budget Exceeded\"`: the bytes this IP has submitted to this route in the current minute, counted by `Content-Length` before the body is read, would pass `ATTACHMENT_UPLOAD_BYTES_PER_MINUTE` (default `268435456`, 256 MiB; `0` turns the budget off). A multipart request without `Content-Length` is charged `ATTACHMENT_MAX_BYTES`. Both carry `Retry-After` with the seconds to wait.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              },
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Attach a file or a link to a task",
        "tags": ["Attachments"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/attachments/{attachmentId}": {
      "get": {
        "operationId": "AttachmentController_findOne",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "attachmentId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AttachmentSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read one attachment",
        "tags": ["Attachments"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "description": "Open to every content role, with no author line — unlike comment deletion. The same role can already delete the whole task and `Attachment.taskId` cascades, so gating the smaller act would be a UI trap rather than an authorization check.",
        "operationId": "AttachmentController_remove",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "attachmentId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Removed. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Detach an attachment",
        "tags": ["Attachments"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/attachments/{attachmentId}/content": {
      "get": {
        "description": "**The one endpoint in this API that does not answer with JSON.** It streams the stored\nfile with the media type sniffed at upload — never the one the client declared.\n\nAsking for the content of a `LINK` is `404`, not `400`: there are no bytes, and saying\n\"wrong kind\" would confirm the row exists.",
        "operationId": "AttachmentController_content",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "attachmentId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "The stored bytes, with the file's own sniffed media type as `Content-Type` — one of the image, PDF, office, ZIP or plain-text families on the upload allowlist, not `application/octet-stream`.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              },
              "Content-Disposition": {
                "description": "`inline` for the four raster image types, so a panel can preview them; `attachment` for everything else, PDFs included.",
                "schema": {
                  "type": "string"
                }
              },
              "Content-Length": {
                "description": "Size in bytes.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-Content-Type-Options": {
                "description": "Always `nosniff`.",
                "schema": {
                  "type": "string"
                }
              },
              "Cross-Origin-Resource-Policy": {
                "description": "Always `same-origin`, overriding the `cross-origin` policy the API sets globally: the web app legitimately reads this API, and that argument does not extend to user-uploaded bytes.",
                "schema": {
                  "type": "string"
                }
              },
              "Cache-Control": {
                "description": "Always `private, max-age=0, must-revalidate`.",
                "schema": {
                  "type": "string"
                }
              }
            },
            "content": {
              "application/octet-stream": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.\n\nNo such attachment in this workspace, **or** the attachment is a `LINK` and therefore has no bytes.",
            "content": {
              "application/octet-stream": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Download an attachment's bytes",
        "tags": ["Attachments"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/activities": {
      "get": {
        "description": "Every write in the workspace, newest first. `payload` varies by `type`; the client renders one translated sentence per type rather than reading free text out of it.",
        "operationId": "ActivityController_listWorkspace",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size. Absent or unusable falls back to 50; anything above 100 is clamped, not rejected.",
            "schema": {
              "minimum": 1,
              "maximum": 100,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "cursor",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivityPageSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Page the workspace's activity feed",
        "tags": ["Activity"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/activities": {
      "get": {
        "operationId": "ActivityController_listForTask",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size. Absent or unusable falls back to 50; anything above 100 is clamped, not rejected.",
            "schema": {
              "minimum": 1,
              "maximum": 100,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "cursor",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivityPageSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Page one task's activity feed",
        "tags": ["Activity"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/config": {
      "get": {
        "description": "Capability, never tenant state — nothing here varies by workspace, role or caller, which is why it carries no `{workspaceId}` and no role gate. It does require a session: the leak is small, but an unauthenticated version would hand a scanner a per-instance list of what a self-hosted install has left unconfigured, and nothing needs it public.",
        "operationId": "InstanceConfigController_config",
        "parameters": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstanceConfigSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read what this deployment is configured to do",
        "tags": ["Instance"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/me": {
      "get": {
        "description": "Sign-in, sign-up and sign-out are Better Auth routes under `/auth/*`, which is mounted below the Nest router and is therefore not described in this document.",
        "operationId": "AuthController_me",
        "parameters": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read the caller's own profile",
        "tags": ["Account"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "patch": {
        "description": "Not workspace-scoped and deliberately not role-gated: the subject is the caller, so the session guard is the whole authorization story. Two fields: the interface language, where `null` is a distinct state from `\"en\"` (it means \"follow the browser\"), and `emailNotifications`, the per-user switch for notification email.",
        "operationId": "AuthController_updateMe",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateMeDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Update the caller's own profile",
        "tags": ["Account"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "operationId": "AccountController_remove",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteAccountDto"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "No Content",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Deletes the caller's account: `204`, and the session it was sent with is over.\n\nThe cookies are cleared on the way out because Better Auth's `session.cookieCache` answers\nfrom a signed cookie for up to 60 seconds without consulting the database, so deleting\nthe `Session` rows does not by itself stop this browser presenting a valid session. Clearing\nthem here closes that window for the browser that asked — which is every browser on this\npath (ADR 0026 §\"Consequences\" covers the administrator path, where it cannot be closed).",
        "tags": ["Account"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/me/deletion-preview": {
      "get": {
        "operationId": "AccountController_preview",
        "parameters": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "What deleting this account would do, before any of it happens.\n\nA separate `GET` rather than a field on `/me`: it runs six counts and a per-workspace\nroster read, and `/me` is on the web's locale-resolution path — it is polled, this is\nopened once by somebody who has decided to read it.",
        "tags": ["Account"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/instance/users/{userId}/deletion-preview": {
      "get": {
        "operationId": "InstanceAccountController_preview",
        "parameters": [
          {
            "name": "userId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "What deleting this account would do.\n\nThe operator reads this before deciding, exactly as the user does — and needs it more,\nbecause they cannot see the person's workspaces any other way. It is also what makes the\n≤30-minute target reachable without a database session: the sole-owned workspaces and\ntheir transfer candidates are on the screen.",
        "tags": ["InstanceAccount"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/instance/users/{userId}": {
      "delete": {
        "operationId": "InstanceAccountController_remove",
        "parameters": [
          {
            "name": "userId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteAccountDto"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "No Content",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Deletes the named account. `204`.\n\n`confirmEmail` has to match the target's address, and on this path it is doing more work\nthan on the self-service one: a UUID in a URL is unreadable, and a mistyped one is a\ndifferent person whose data would be destroyed instead.",
        "tags": ["InstanceAccount"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces": {
      "get": {
        "description": "The one collection in this API with no `:workspaceId` above it — it *is* how a caller discovers their tenants. Bounded by membership, so it is a plain array.",
        "operationId": "WorkspaceController_list",
        "parameters": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/WorkspaceSchema"
                  }
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "List the workspaces the caller belongs to",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "description": "The caller becomes its `OWNER`. A duplicate `slug` is `409`.",
        "operationId": "WorkspaceController_create",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateWorkspaceDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Create a workspace",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}": {
      "get": {
        "operationId": "WorkspaceController_get",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read one workspace",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "patch": {
        "operationId": "WorkspaceController_update",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateWorkspaceDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Rename a workspace, or change its slug",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "description": "OWNER only — the one route in the API gated on a single role rather than a role set. Cascades to every board, task and file in it.",
        "operationId": "WorkspaceController_remove",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a workspace",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/members/me": {
      "get": {
        "description": "A caller that only needs its own role should never page the roster to find it.",
        "operationId": "WorkspaceController_getOwnMembership",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceMemberSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read the caller's own membership",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/members/me/leave": {
      "post": {
        "description": "Self-service and deliberately not role-gated: a GUEST invited by mistake must be able to walk out without asking an admin. `204` because the membership it acted on is gone.",
        "operationId": "WorkspaceController_leaveWorkspace",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Left. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Leave a workspace",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/members": {
      "get": {
        "description": "A cursor page whose `limit` defaults to the 100 ceiling, so an ordinary workspace is one request answering `hasMore: false`. It is paginated at all because \"members are always few\" is an expectation rather than a construction — a plain array behind `take: 1000` lost a large workspace its tail with nothing in the response saying so.",
        "operationId": "WorkspaceController_listMembers",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size. Absent or unusable falls back to 100; anything above 100 is clamped, not rejected.",
            "schema": {
              "minimum": 1,
              "maximum": 100,
              "default": 100,
              "type": "number"
            }
          },
          {
            "name": "cursor",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceMemberPageSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Page the member roster",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/members/{userId}": {
      "delete": {
        "description": "Addressed by `userId` rather than by membership id: the caller knows *who*, and the row id is an implementation detail of the roster response.",
        "operationId": "WorkspaceController_removeMember",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "userId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Removed. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Remove a member",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/members/{userId}/role": {
      "patch": {
        "description": "`/role` rather than `PATCH .../members/{userId}`: role is the only mutable field of a membership, and the sub-resource says so instead of leaving callers to discover that every other key is rejected.",
        "operationId": "WorkspaceController_updateMemberRole",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "userId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateMemberRoleDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceMemberSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Change a member's role",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/invitations": {
      "get": {
        "description": "Admin-only, unlike the roster beside it. A member is visible by their own decision; an invited address belongs to someone who has agreed to nothing yet, and publishing the queue to every GUEST would hand out contact details nobody consented to share.",
        "operationId": "WorkspaceController_listInvitations",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size. Absent or unusable falls back to 100; anything above 100 is clamped, not rejected.",
            "schema": {
              "minimum": 1,
              "maximum": 100,
              "default": 100,
              "type": "number"
            }
          },
          {
            "name": "cursor",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InvitationPageSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Page the pending invitations",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "description": "The response carries `emailDelivery`, and **an absent field is not `SENT`** — it means this API observed no send. Delivery never fails the request: on a deployment with no SMTP the `acceptUrl` in the body is the path that works. Rate limited well below the default, because each call hands a message to the relay aimed at an address the caller chooses.",
        "operationId": "WorkspaceController_createInvitation",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateInvitationDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InvitationSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Invite somebody to the workspace",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/invitations/{invitationId}": {
      "delete": {
        "operationId": "WorkspaceController_revokeInvitation",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "invitationId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Revoked. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Revoke a pending invitation",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/invitations/{invitationId}/accept": {
      "post": {
        "description": "Session-authenticated but **not** membership-gated, and the only workspace-scoped route that is not: the invitee is not a member until this succeeds. `200` rather than `201` because it acts on an existing invitation, and the membership it returns has no URL of its own for a `Location` header to point at.",
        "operationId": "WorkspaceController_acceptInvitation",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "invitationId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkspaceMemberSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Accept an invitation",
        "tags": ["Workspaces"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/boards": {
      "get": {
        "operationId": "BoardController_list",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/BoardSchema"
                  }
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "List the workspace’s boards",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "description": "The board arrives with no columns. `Accept-Language` is read only as the fallback for a creator who has set no locale, because the default column names are written into the database rather than rendered.",
        "operationId": "BoardController_create",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "accept-language",
            "required": true,
            "in": "header",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateBoardDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BoardSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Create a board",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/boards/{boardId}": {
      "get": {
        "description": "Also records the `wau_board_view` activation signal — one row per user, per workspace, per UTC day. It carries the workspace and never the board.",
        "operationId": "BoardController_get",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BoardSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read one board",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "patch": {
        "description": "Only the fields present change; an explicit `null` clears a nullable one.",
        "operationId": "BoardController_update",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateBoardDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BoardSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Update a board",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "description": "Cascades to its columns, tasks and everything hanging off them.",
        "operationId": "BoardController_remove",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a board",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/boards/{boardId}/columns": {
      "get": {
        "description": "Not paginated. A board holds a bounded number of columns by construction, which is the only condition under which this API returns a plain array.",
        "operationId": "ColumnController_list",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ColumnSchema"
                  }
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "List a board's columns",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "operationId": "ColumnController_create",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateColumnDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ColumnSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Create a column",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/boards/{boardId}/columns/defaults": {
      "post": {
        "description": "One transaction. Same gate as creating a single column, because that is what it does. An action rather than a resource, hence `201` with the columns it wrote.",
        "operationId": "ColumnController_createDefaults",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "accept-language",
            "required": true,
            "in": "header",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ColumnSchema"
                  }
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Seed an empty board with the starting columns",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/columns/{columnId}": {
      "patch": {
        "operationId": "ColumnController_update",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "columnId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateColumnDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ColumnSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Update a column",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "operationId": "ColumnController_remove",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "columnId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a column",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/columns/{columnId}/position": {
      "patch": {
        "description": "Position is a sub-resource with a verb-free name rather than an action segment. The new `position` is a fractional index computed from the neighbours named in the body — never an integer, never contiguous.",
        "operationId": "ColumnController_move",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "columnId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MoveColumnDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ColumnSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Reorder a column",
        "tags": ["Boards"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/instance/activation": {
      "get": {
        "description": "The only route outside `/workspaces/{workspaceId}` that reads product data, and the only one gated on `INSTANCE_ADMIN_EMAILS` rather than on a workspace role: it answers \"how is this deployment doing\", a question no tenant owns. **Nothing it returns leaves the instance** — these are aggregates over local rows, and the one outbound path in this codebase is off by default and carries none of them.",
        "operationId": "ActivationController_funnel",
        "parameters": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivationFunnelSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "Signed in, but the account is not listed in `INSTANCE_ADMIN_EMAILS`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read this instance's activation funnel",
        "tags": ["Instance"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/boards/{boardId}/tasks": {
      "get": {
        "description": "A cursor page **walked by `id`, never by `position`** — fractional indexing rewrites\n`position` on every drag, so a cursor keyed on it would never return a row someone moved\npast the window again. Sort the accumulated set by `position` for display.\n\nFilters combine with AND; repeated values within one filter are OR. An unknown query key\nis a `400` rather than a silently dropped filter.\n\n`?q=` runs a trigram scan over title and description and carries a tighter rate limit\nthan the same route without it.",
        "operationId": "TaskController_list",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size. Absent or unusable falls back to 50; anything above 100 is clamped, not rejected.",
            "schema": {
              "minimum": 1,
              "maximum": 100,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "cursor",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "q",
            "required": false,
            "in": "query",
            "schema": {
              "maxLength": 200,
              "type": "string"
            }
          },
          {
            "name": "priority",
            "required": false,
            "in": "query",
            "schema": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": ["LOW", "MEDIUM", "HIGH", "URGENT"]
              }
            }
          },
          {
            "name": "assigneeId",
            "required": false,
            "in": "query",
            "description": "Assignee user ids (OR), or the literal `null` for unassigned.\nMixed `null` + ids means unassigned OR those assignees.",
            "schema": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          {
            "name": "labelId",
            "required": false,
            "in": "query",
            "schema": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          {
            "name": "dueDate",
            "required": false,
            "in": "query",
            "description": "Only the string `null` — tasks with no due date. Ranges use bracket keys.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "dueDate[gte]",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "dueDate[lte]",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskPageSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "List a board's tasks",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "operationId": "TaskController_create",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateTaskDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Create a task in a board",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}": {
      "get": {
        "description": "The only read that carries full `checklists`; a list read fills `checklistSummary` and leaves `checklists` `null`.",
        "operationId": "TaskController_get",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read one task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "patch": {
        "description": "Only the fields present change. Sending `null` explicitly clears a nullable field; omitting it leaves the field untouched.",
        "operationId": "TaskController_update",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateTaskDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Update a task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "description": "Cascades to its comments, checklists and attachments.",
        "operationId": "TaskController_remove",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/position": {
      "patch": {
        "description": "Column and ordering in one request. The new `position` is a fractional index computed from the neighbours named in the body, so concurrent moves do not have to renumber the column. Moving to a column on another board is `422`.",
        "operationId": "TaskController_move",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MoveTaskDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Move a task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/assignees": {
      "post": {
        "description": "Answers with the whole task, so a client never stitches two responses together.",
        "operationId": "TaskController_addAssignee",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddAssigneeDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Assign a member to a task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/assignees/{userId}": {
      "delete": {
        "description": "Answers `200` with the task rather than `204`: the assignment is a sub-resource of the task, and the caller needs the updated task either way.",
        "operationId": "TaskController_removeAssignee",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "userId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Unassign a member",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/labels": {
      "post": {
        "operationId": "TaskController_addLabel",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddTaskLabelDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Attach a label to a task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/labels/{labelId}": {
      "delete": {
        "description": "Answers `200` with the task. The label itself is untouched.",
        "operationId": "TaskController_removeLabel",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "labelId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Detach a label from a task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/checklists": {
      "post": {
        "description": "There is no checklist read endpoint: every mutation here answers with the freshly-read task, and `GET tasks/{taskId}` carries the checklists in full.",
        "operationId": "TaskController_createChecklist",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateChecklistDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Add a checklist to a task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/checklists/{checklistId}": {
      "patch": {
        "operationId": "TaskController_updateChecklist",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "checklistId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateChecklistDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Rename a checklist",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "description": "Answers `200` with the task, not `204` — the same exception every checklist route makes, and for the same reason: the card badge changes and the caller needs the new one.",
        "operationId": "TaskController_removeChecklist",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "checklistId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a checklist",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/checklists/{checklistId}/position": {
      "patch": {
        "operationId": "TaskController_moveChecklist",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "checklistId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MoveChecklistDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Reorder a checklist within its task",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/checklists/{checklistId}/items": {
      "post": {
        "operationId": "TaskController_createChecklistItem",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "checklistId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateChecklistItemDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Add an item to a checklist",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/checklist-items/{itemId}": {
      "patch": {
        "description": "Addressed by its own id rather than through its checklist — the id already identifies the row and the workspace guard already scopes it.",
        "operationId": "TaskController_updateChecklistItem",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "itemId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateChecklistItemDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Edit a checklist item, or tick it",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "operationId": "TaskController_removeChecklistItem",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "itemId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a checklist item",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/checklist-items/{itemId}/position": {
      "patch": {
        "operationId": "TaskController_moveChecklistItem",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "itemId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MoveChecklistItemDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Reorder a checklist item",
        "tags": ["Tasks"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/notifications": {
      "get": {
        "description": "Scoped to the caller as well as to the workspace — a recipient never sees another member's rows, whatever their role.",
        "operationId": "NotificationController_list",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size. Absent or unusable falls back to 50; anything above 100 is clamped, not rejected.",
            "schema": {
              "minimum": 1,
              "maximum": 100,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "cursor",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "unreadOnly",
            "required": false,
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "type",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string",
              "enum": ["assignment", "mention", "due_soon"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotificationPageSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Page the caller's notifications",
        "tags": ["Notifications"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/notifications/unread-count": {
      "get": {
        "description": "A separate endpoint so the bell badge does not have to page the list. The socket event `notification:unread-changed` tells a connected client when to re-read it.",
        "operationId": "NotificationController_unreadCount",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotificationUnreadCountSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Count unread notifications",
        "tags": ["Notifications"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/notifications/read-all": {
      "post": {
        "description": "An action, so `200` with what it did rather than `201`. `updated: 0` means everything was already read and is not an error.",
        "operationId": "NotificationController_markAllRead",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MarkAllReadSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Mark every notification read",
        "tags": ["Notifications"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/notifications/{notificationId}/read": {
      "post": {
        "description": "Answers with the row, so the caller can read the `readAt` the server stamped.",
        "operationId": "NotificationController_markRead",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "notificationId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NotificationSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Mark one notification read",
        "tags": ["Notifications"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/boards/{boardId}/labels": {
      "get": {
        "operationId": "LabelController_list",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/LabelSchema"
                  }
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "List a board's labels",
        "tags": ["Labels"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "description": "`color` is a design-token slot name (`slot-1`…`slot-8`) resolved by the theme, never a raw hex value — a stored hex cannot be legible in both light and dark.",
        "operationId": "LabelController_create",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateLabelDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LabelSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Create a label",
        "tags": ["Labels"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/labels/{labelId}": {
      "patch": {
        "operationId": "LabelController_update",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "labelId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateLabelDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LabelSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Rename or recolour a label",
        "tags": ["Labels"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "delete": {
        "description": "Detaches it from every task that carries it.",
        "operationId": "LabelController_remove",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "labelId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a label",
        "tags": ["Labels"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/tasks/{taskId}/comments": {
      "get": {
        "description": "Cursor-paginated, keyed on `id`. `limit` defaults to the 100 ceiling.",
        "operationId": "CommentController_list",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "Page size. Absent or unusable falls back to 100; anything above 100 is clamped, not rejected.",
            "schema": {
              "minimum": 1,
              "maximum": 100,
              "default": 100,
              "type": "number"
            }
          },
          {
            "name": "cursor",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommentPageSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Page a task's comments",
        "tags": ["Comments"],
        "security": [
          {
            "session": []
          }
        ]
      },
      "post": {
        "operationId": "CommentController_create",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "taskId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateCommentDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommentSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Comment on a task",
        "tags": ["Comments"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/comments/{commentId}": {
      "delete": {
        "description": "The author, or an `OWNER`/`ADMIN`. A comment is a person’s statement, which is why this draws an author line that attachment deletion deliberately does not.",
        "operationId": "CommentController_remove",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "commentId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted. Empty body.",
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN, MEMBER.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Delete a comment",
        "tags": ["Comments"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/imports/trello": {
      "post": {
        "description": "Takes a Trello board's JSON export and creates a **new board** from it. The API's only\nbulk write, and its only endpoint whose collection segment names no readable resource:\nthere is no `GET /imports` and no import id.\n\n**Multipart rather than JSON, deliberately.** A real export is several megabytes and\n`REQUEST_BODY_MAX_BYTES` is 1 MiB; raising that to fit this one endpoint would hand the\nsame memory cost to every other endpoint. So the export arrives as a file part under a\nceiling this module owns, and the two numbers measure different things —\n`TRELLO_IMPORT_MAX_BYTES` is a **heap** ceiling, `ATTACHMENT_MAX_BYTES` is a **disk** one.\n\n**`OWNER`/`ADMIN` by permission arithmetic.** Creating a board is a content role, but\ncreating a *column* is admin-only, and an import creates both. An endpoint must not do in\none request what its caller could not do in several.\n\n**Not idempotent.** Posting the same export twice creates two boards. There is no dedupe\nkey, no update-in-place and no \"already imported\" answer.\n\nWhat it deliberately does not carry across: member assignments, comments, column\ncategories (every imported column arrives `UNSTARTED`) and attachment *bytes* — a Trello\nexport carries URLs, so every attachment becomes a `LINK`. All of it is counted in the\nreport rather than dropped silently.",
        "operationId": "ImportController_trello",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "type": "object",
                "required": ["file"],
                "properties": {
                  "file": {
                    "type": "string",
                    "format": "binary",
                    "description": "The board's JSON export. The only part this endpoint reads; there is no other field and no JSON body shape."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The board was written, atomically. **This response is the whole report and it is stored nowhere** — a caller that discards it has lost the list of what did not come across.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TrelloImportReportSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "400": {
            "description": "No part named `file`; the file is not valid JSON; or the JSON is not a Trello board export. **Nothing is written when this happens** — the export is read and mapped in full before the transaction opens.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "403": {
            "description": "A member of the workspace whose role is not one of: OWNER, ADMIN.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "413": {
            "description": "The file part is over `TRELLO_IMPORT_MAX_BYTES` (default `20971520` — 20 MiB). This is this module's own limit, not the attachment ceiling and not `REQUEST_BODY_MAX_BYTES`: an import stores no bytes at all, so it must keep working on an instance with attachments switched off.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Import a Trello board export",
        "tags": ["Import"],
        "security": [
          {
            "session": []
          }
        ]
      }
    },
    "/workspaces/{workspaceId}/dashboard/summary": {
      "get": {
        "description": "Computed per request. `byColumn` is present only when `?boardId=` is set.\n\n**`byAssignee[].count` is assignments, not tasks.** A task with three assignees\ncontributes one to each, so the sum deliberately exceeds `totalTasks` on a board that\nuses multiple assignees; `Unassigned` is the exception and is a task count.\n\nAlso records the `dashboard_viewed` activation step — here rather than from the\nbrowser, because reaching this handler *is* the view.",
        "operationId": "DashboardController_summary",
        "parameters": [
          {
            "name": "workspaceId",
            "required": true,
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "UUIDv7. Opaque — never parsed, sorted or generated client-side."
          },
          {
            "name": "boardId",
            "required": false,
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DashboardSummarySchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "401": {
            "description": "No session cookie, or one the server no longer recognises.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "404": {
            "description": "The workspace does not exist, or the caller is not a member of it, or the addressed resource belongs to another workspace. All three answer `404` — a `403` would confirm that something exists across the tenant boundary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            },
            "headers": {
              "X-Request-Id": {
                "description": "Correlation id for this request — the same value as the error envelope `requestId` and the server log lines. Echoed from the request when the client supplied a URL-safe value of 8-128 characters, generated otherwise.",
                "schema": {
                  "type": "string"
                }
              },
              "X-RateLimit-Limit": {
                "description": "Requests allowed for this route, in the current window.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Remaining": {
                "description": "Requests left in the current window. Never negative.",
                "schema": {
                  "type": "integer"
                }
              },
              "X-RateLimit-Reset": {
                "description": "Seconds until the current window expires.",
                "schema": {
                  "type": "integer"
                }
              }
            }
          },
          "429": {
            "description": "Over this route's per-IP budget. Carries `Retry-After` with the seconds to wait.",
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before retrying.",
                "schema": {
                  "type": "integer"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          },
          "500": {
            "description": "Unhandled failure. Never carries a stack trace, and never the raw exception message under `NODE_ENV=production`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelopeSchema"
                }
              }
            }
          }
        },
        "summary": "Read the workspace dashboard aggregates",
        "tags": ["Dashboard"],
        "security": [
          {
            "session": []
          }
        ]
      }
    }
  },
  "info": {
    "title": "Kurul API",
    "description": "The REST contract of one Kurul instance, generated from the running application: every path,\nparameter, request body and response below is what the NestJS router and the DTO classes\nactually declare, not a hand-maintained copy of them.\n\nThe prose contract is [`docs/api-conventions.md`](https://github.com/dravcore/kurul/blob/develop/docs/api-conventions.md).\nWhere this document and that one disagree, one of them is wrong and neither wins by default.\n\n### What is not in here\n\n**`/auth/*`.** Better Auth is mounted on raw Express below the Nest router\n([ADR 0004](https://github.com/dravcore/kurul/blob/develop/docs/decisions/0004-auth-better-auth.md)),\nso it has no controller to scan and no route this document can discover. Sign-in, sign-up,\nsign-out and session refresh live there. **The Socket.io contract** is likewise absent: it is\nnot HTTP, and it is defined in `@kurul/shared-types`.\n\n### Authentication\n\nA **session cookie**, issued by `POST /auth/sign-in/email`. Every operation requires it except\nthe two health probes. A browser on the allowed origin sends it automatically; any other client\nmust store and replay it.\n\nWrites (`POST`/`PUT`/`PATCH`/`DELETE`) are additionally checked server-side against an\norigin allowlist. A request that announces an `Origin` other than `WEB_URL` is refused with\n`403` before it reaches a handler; a request that announces none — `curl`, a CI script, a\nnative client — is allowed, because no cross-site request shape carries a victim's cookie *and*\nomits the header.\n\n### Errors\n\nOne shape, everywhere, including failures nobody wrote a handler for: `ErrorEnvelopeSchema`.\nThere is no second error format. Clients branch on `statusCode` and `error`, never on\n`message` text. The single documented exception is `GET /health/ready`, whose `503` carries\nthe probe document instead — its caller is a healthcheck, not a client.\n\n**`404`, not `403`, across the tenant boundary.** A resource in another workspace, a\nworkspace the caller is not a member of, and a workspace that does not exist are all `404`.\n`403` means \"a member, whose role is too low\", and nothing else.\n\n### Pagination\n\nCursor pagination is the default, and **the cursor key is always `id`, never `position`**.\nThat is a correctness rule: fractional indexing rewrites `position` on every drag, so a row\nsomeone moves past the client's window would never be returned again. `limit` defaults to 50\n(100 on the member and invitation rosters) and is **clamped** at 100 rather than rejected.\n`nextCursor` is opaque — it is the `id` of the last item, and clients must not parse it.\n\n### Rate limiting\n\nPer client IP **and** per route, over a rolling 60-second window, so\none endpoint running hot never spends another's allowance. Responses under budget carry\n`X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Reset`; a `429` carries\n`Retry-After`. All four are absent when `RATE_LIMIT_ENABLED=false` — supported for the\nintegration suite and for nothing else, since a deployment that sets it has no brute-force\nceiling at all.\n\n| Route | Budget |\n| --- | --- |\n| Anything not listed below | 100 / min |\n| `POST /workspaces/{workspaceId}/invitations` | 10 / min |\n| `GET /workspaces/{workspaceId}/boards/{boardId}/tasks?q=` | 30 / min |\n| `POST /workspaces/{workspaceId}/tasks/{taskId}/attachments` | 20 / min |\n| `POST /workspaces/{workspaceId}/imports/trello` | 3 / min |\n| `GET /workspaces/{workspaceId}/attachments/{attachmentId}/content` | 300 / min |\n| `GET /health`, `GET /health/ready` | exempt |\n\n`/auth/*` is governed by a second, independent limiter inside Better Auth and is not covered\nby the headers above.\n\n### Correlation\n\nEvery response carries `X-Request-Id`. A client may supply its own if it is URL-safe and 8-128\ncharacters; anything else is replaced with a generated UUIDv7. The same value appears in the\nerror envelope's `requestId` and in the server's log lines, so one id selects one request.",
    "version": "0.1.0",
    "contact": {},
    "license": {
      "name": "AGPL-3.0-only",
      "url": "https://www.gnu.org/licenses/agpl-3.0.en.html"
    }
  },
  "tags": [
    {
      "name": "Instance",
      "description": "Liveness, readiness, deployment capabilities and the activation funnel. The only unauthenticated routes in the API are the two probes."
    },
    {
      "name": "Account",
      "description": "The caller's own profile."
    },
    {
      "name": "Workspaces",
      "description": "Workspaces, the member roster and invitations — the tenant root."
    },
    {
      "name": "Boards",
      "description": "Boards and their columns."
    },
    {
      "name": "Tasks",
      "description": "Tasks, ordering, assignees, task labels and checklists."
    },
    {
      "name": "Labels",
      "description": "Board-scoped labels."
    },
    {
      "name": "Comments",
      "description": "Task comments."
    },
    {
      "name": "Attachments",
      "description": "Files and links on a task, and the one endpoint in this API that answers with something other than JSON."
    },
    {
      "name": "Import",
      "description": "The API's only bulk write: a Trello board export."
    },
    {
      "name": "Activity",
      "description": "Workspace and task activity feeds."
    },
    {
      "name": "Notifications",
      "description": "In-app notifications."
    },
    {
      "name": "Dashboard",
      "description": "Read-only aggregations over a workspace."
    }
  ],
  "servers": [
    {
      "url": "/",
      "description": "This instance"
    }
  ],
  "components": {
    "securitySchemes": {
      "session": {
        "type": "apiKey",
        "in": "cookie",
        "name": "better-auth.session_token",
        "description": "Session cookie issued by `POST /auth/sign-in/email`, which is served below the Nest router and is therefore not described in this document."
      }
    },
    "schemas": {
      "LivenessSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "description": "Always `ok`. A body at all only so the probe has something to read."
          }
        },
        "required": ["status"]
      },
      "ReadinessChecksSchema": {
        "type": "object",
        "properties": {
          "database": {
            "type": "string",
            "enum": ["up", "down", "skipped"]
          },
          "redis": {
            "type": "string",
            "enum": ["up", "down", "skipped"]
          }
        },
        "required": ["database", "redis"]
      },
      "ReadinessSchema": {
        "type": "object",
        "properties": {
          "status": {
            "enum": ["error", "ok"],
            "type": "string"
          },
          "checks": {
            "$ref": "#/components/schemas/ReadinessChecksSchema"
          }
        },
        "required": ["status", "checks"]
      },
      "AttachmentSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "taskId": {
            "type": "string"
          },
          "kind": {
            "enum": ["FILE", "LINK"],
            "type": "string"
          },
          "filename": {
            "type": "string",
            "description": "Display name. For a FILE this is what the browser sent; it never appears in a path."
          },
          "mimeType": {
            "type": "string",
            "nullable": true,
            "description": "FILE only: the **sniffed** media type, never the one the client declared at upload."
          },
          "size": {
            "type": "number",
            "nullable": true,
            "description": "FILE only: size in bytes."
          },
          "url": {
            "type": "string",
            "nullable": true,
            "description": "LINK only: an `http:`/`https:` URL the server has never requested and never will."
          },
          "uploadedById": {
            "type": "string"
          },
          "createdAt": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "taskId",
          "kind",
          "filename",
          "mimeType",
          "size",
          "url",
          "uploadedById",
          "createdAt"
        ]
      },
      "ValidationDetailSchema": {
        "type": "object",
        "properties": {
          "field": {
            "type": "string",
            "description": "Property path that failed, as sent — `title`, `assignees.0.userId`."
          },
          "constraint": {
            "type": "string",
            "description": "The class-validator rule that rejected it (`isNotEmpty`, `min`, `isUuid`).\n\nAbsent when the failure came from an exception thrown without going through\n`validationExceptionFactory`, where only the message survives."
          },
          "message": {
            "type": "string",
            "description": "Human-readable sentence for this one field."
          }
        },
        "required": ["field", "message"]
      },
      "ErrorEnvelopeSchema": {
        "type": "object",
        "properties": {
          "statusCode": {
            "type": "number",
            "description": "Mirrors the HTTP status of the response."
          },
          "error": {
            "type": "string",
            "description": "Stable reason phrase for the status — `Bad Request`, `Not Found`, `Payload Too Large`.\n\nNode's own `http.STATUS_CODES` table, not the `HttpStatus` enum member name, so it is the\nphrase a client can match on rather than `NOT_FOUND`."
          },
          "message": {
            "type": "string",
            "description": "One human-readable sentence, safe to log.\n\nNever a raw exception string under `NODE_ENV=production`, and never a stack trace anywhere."
          },
          "details": {
            "description": "Per-field problems. Present on validation failures (`400`, `422`) and nowhere else.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValidationDetailSchema"
            }
          },
          "path": {
            "type": "string",
            "description": "The request path that failed, query string included."
          },
          "timestamp": {
            "type": "string",
            "description": "ISO 8601 UTC instant the envelope was written."
          },
          "requestId": {
            "type": "string",
            "description": "Correlation id — the same value as the `X-Request-Id` response header and as the server's\nlog lines for this request. A user reporting a failure quotes one id and it selects exactly\none request."
          }
        },
        "required": ["statusCode", "error", "message", "path", "timestamp"]
      },
      "AuthorSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "avatarUrl": {
            "type": "string",
            "nullable": true
          },
          "deleted": {
            "type": "boolean",
            "description": "True when the account has been anonymised (ADR 0026). `name` then carries the stored\ntombstone rather than a person's name, and a client is expected to render its own label\ninstead — which is why this is a flag rather than a string comparison against `name`."
          }
        },
        "required": ["id", "name", "avatarUrl", "deleted"]
      },
      "ActivitySchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "workspaceId": {
            "type": "string"
          },
          "taskId": {
            "type": "string",
            "nullable": true
          },
          "userId": {
            "type": "string"
          },
          "type": {
            "type": "string",
            "description": "`ActivityType` — e.g. `task.created`, `task.moved`, `board.imported`."
          },
          "payload": {
            "type": "object",
            "additionalProperties": true,
            "description": "Shape varies by `type`; the client renders one sentence per type."
          },
          "createdAt": {
            "type": "string"
          },
          "author": {
            "$ref": "#/components/schemas/AuthorSchema"
          }
        },
        "required": [
          "id",
          "workspaceId",
          "taskId",
          "userId",
          "type",
          "payload",
          "createdAt",
          "author"
        ]
      },
      "ActivityPageSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActivitySchema"
            }
          },
          "nextCursor": {
            "type": "string",
            "nullable": true
          },
          "hasMore": {
            "type": "boolean"
          }
        },
        "required": ["items", "nextCursor", "hasMore"]
      },
      "InstanceConfigSchema": {
        "type": "object",
        "properties": {
          "mailEnabled": {
            "type": "boolean",
            "description": "`false` when no SMTP host is configured: mail is written to the log and delivered nowhere."
          },
          "attachmentsEnabled": {
            "type": "boolean",
            "description": "`false` when `STORAGE_PATH` is unset, so this deployment stores no files.\n\nLink attachments do not depend on it — a `LINK` needs no storage at all."
          }
        },
        "required": ["mailEnabled", "attachmentsEnabled"]
      },
      "UserSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "email": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "avatarUrl": {
            "type": "string",
            "nullable": true
          },
          "locale": {
            "nullable": true,
            "description": "Chosen interface language as an IETF tag, or `null` for \"never chose\".\n\n`null` is a distinct state from `\"en\"`: an unset user follows their browser's\n`Accept-Language`.",
            "enum": ["en", "tr"],
            "type": "string"
          },
          "emailNotifications": {
            "type": "boolean",
            "description": "Whether assignment, mention and due-soon notifications are also sent by email. One switch\nfor every kind. Has no effect while `InstanceConfigDto.mailEnabled` is `false`."
          },
          "createdAt": {
            "type": "string",
            "description": "ISO 8601 UTC."
          }
        },
        "required": [
          "id",
          "email",
          "name",
          "avatarUrl",
          "locale",
          "emailNotifications",
          "createdAt"
        ]
      },
      "UpdateMeDto": {
        "type": "object",
        "properties": {
          "locale": {
            "type": "string",
            "nullable": true,
            "description": "Nullable on purpose: clearing the preference puts the user back on their browser's\n`Accept-Language`, which a user who set a language on someone else's machine should be\nable to undo. Omitting the key leaves the stored value alone.\n\nValidated against `SUPPORTED_LOCALES` rather than a loose IETF-tag pattern, so the column\ncan only ever hold a tag the app actually ships a catalog for.",
            "enum": ["en", "tr"]
          },
          "emailNotifications": {
            "type": "boolean",
            "description": "Not nullable: the column has a default and \"no preference\" is not a state it can hold.\nOmitting the key leaves the stored value alone."
          }
        }
      },
      "WorkspaceDispositionDto": {
        "type": "object",
        "properties": {
          "workspaceId": {
            "type": "string",
            "format": "uuid"
          },
          "action": {
            "type": "string",
            "enum": ["transfer", "delete"]
          },
          "newOwnerUserId": {
            "type": "string",
            "description": "Required for `transfer`, refused for `delete`.\n\n`ValidateIf` on the action rather than `@IsOptional()`: a body that says `delete` and also\nnames a new owner is not a `delete` request with a harmless extra field, it is a request\nwhose author believed two different things. `whitelist: true` strips unknown keys but not\ndeclared ones, so this is what makes the union actually exclusive.",
            "format": "uuid"
          }
        },
        "required": ["workspaceId", "action"]
      },
      "DeleteAccountDto": {
        "type": "object",
        "properties": {
          "confirmEmail": {
            "type": "string",
            "description": "The address of the account being deleted, typed back by the caller.\n\nA misclick gate and documented as nothing more: the session sending this can already delete\nevery workspace the user owns, so a password re-prompt here and nowhere else would buy no\nsecurity while implying it had (ADR 0026 §4). On the administrator path it is the operator\nconfirming *which* account they mean, which is the failure worth guarding — an id in a URL\nis unreadable and a mistyped one is another person.",
            "format": "email"
          },
          "dispositions": {
            "description": "One entry per workspace the user is the only OWNER of. Omitted is the same as empty, and\nboth are refused with `409` when there is any such workspace.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WorkspaceDispositionDto"
            }
          }
        },
        "required": ["confirmEmail"]
      },
      "WorkspaceSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "slug": {
            "type": "string",
            "description": "Lowercase alphanumeric with optional hyphens. Unique across the instance."
          },
          "createdAt": {
            "type": "string"
          }
        },
        "required": ["id", "name", "slug", "createdAt"]
      },
      "CreateWorkspaceDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 80
          },
          "slug": {
            "type": "string",
            "minLength": 2,
            "maxLength": 48,
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          }
        },
        "required": ["name", "slug"]
      },
      "UpdateWorkspaceDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 80
          },
          "slug": {
            "type": "string",
            "minLength": 2,
            "maxLength": 48,
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          }
        }
      },
      "WorkspaceMemberSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "workspaceId": {
            "type": "string"
          },
          "userId": {
            "type": "string"
          },
          "role": {
            "enum": ["OWNER", "ADMIN", "MEMBER", "GUEST"],
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "avatarUrl": {
            "type": "string",
            "nullable": true
          }
        },
        "required": ["id", "workspaceId", "userId", "role", "name", "avatarUrl"]
      },
      "WorkspaceMemberPageSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WorkspaceMemberSchema"
            }
          },
          "nextCursor": {
            "type": "string",
            "nullable": true,
            "description": "Pass as `?cursor=` to fetch the next page. `null` on the last page."
          },
          "hasMore": {
            "type": "boolean"
          }
        },
        "required": ["items", "nextCursor", "hasMore"]
      },
      "UpdateMemberRoleDto": {
        "type": "object",
        "properties": {
          "role": {
            "type": "string",
            "enum": ["OWNER", "ADMIN", "MEMBER", "GUEST"]
          }
        },
        "required": ["role"]
      },
      "InvitationSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "workspaceId": {
            "type": "string"
          },
          "email": {
            "type": "string"
          },
          "role": {
            "enum": ["OWNER", "ADMIN", "MEMBER", "GUEST"],
            "type": "string"
          },
          "status": {
            "enum": ["accepted", "canceled", "pending", "rejected"],
            "type": "string"
          },
          "expiresAt": {
            "type": "string"
          },
          "acceptUrl": {
            "type": "string",
            "description": "Computed convenience URL for the client. Not a stored column."
          },
          "emailDelivery": {
            "description": "What became of the invitation email — on `POST .../invitations` and nowhere else.\n\n**Absent is not `SENT`.** It means this API observed no send for the request. A listed\ninvitation is a stored row while delivery is an event nothing records, so\n`GET .../invitations` never carries the field.",
            "enum": ["SENT", "NOT_CONFIGURED", "FAILED"],
            "type": "string"
          }
        },
        "required": ["id", "workspaceId", "email", "role", "status", "expiresAt", "acceptUrl"]
      },
      "InvitationPageSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InvitationSchema"
            }
          },
          "nextCursor": {
            "type": "string",
            "nullable": true
          },
          "hasMore": {
            "type": "boolean"
          }
        },
        "required": ["items", "nextCursor", "hasMore"]
      },
      "CreateInvitationDto": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email"
          },
          "role": {
            "type": "string",
            "enum": ["OWNER", "ADMIN", "MEMBER", "GUEST"]
          }
        },
        "required": ["email", "role"]
      },
      "BoardSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "workspaceId": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string",
            "nullable": true
          },
          "createdAt": {
            "type": "string"
          }
        },
        "required": ["id", "workspaceId", "name", "description", "createdAt"]
      },
      "CreateBoardDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "description": {
            "type": "string",
            "maxLength": 2000
          }
        },
        "required": ["name"]
      },
      "UpdateBoardDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "description": {
            "type": "string",
            "nullable": true,
            "maxLength": 2000
          }
        }
      },
      "ColumnSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "boardId": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "position": {
            "type": "number",
            "description": "Fractional index — a `Float`, never an integer and never contiguous.\n\nRewritten on every reorder, which is why no cursor is ever keyed on it."
          },
          "color": {
            "type": "string",
            "nullable": true
          },
          "category": {
            "description": "Semantic state, independent of `name` and of position. Metrics key off this; a client that\nwants to know whether a column means \"finished\" reads `category`, never the typed label.",
            "enum": ["BACKLOG", "UNSTARTED", "STARTED", "COMPLETED", "CANCELED"],
            "type": "string"
          },
          "taskCount": {
            "type": "number"
          }
        },
        "required": ["id", "boardId", "name", "position", "color", "category", "taskCount"]
      },
      "CreateColumnDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "afterColumnId": {
            "type": "string",
            "nullable": true
          },
          "color": {
            "type": "string",
            "maxLength": 32
          },
          "category": {
            "type": "string",
            "description": "Omitted falls through to the schema default (`UNSTARTED`), not to a name-derived guess.",
            "enum": ["BACKLOG", "UNSTARTED", "STARTED", "COMPLETED", "CANCELED"]
          }
        },
        "required": ["name"]
      },
      "UpdateColumnDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "color": {
            "type": "string",
            "nullable": true,
            "maxLength": 32
          },
          "category": {
            "type": "string",
            "description": "Non-nullable: a column always has a category. Clearing one back to \"unknown\" is not a\nstate the metrics layer has an answer for, so the client picks a value or omits the field.",
            "enum": ["BACKLOG", "UNSTARTED", "STARTED", "COMPLETED", "CANCELED"]
          }
        }
      },
      "MoveColumnDto": {
        "type": "object",
        "properties": {
          "beforeColumnId": {
            "type": "string",
            "nullable": true
          },
          "afterColumnId": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "ActivationStepSchema": {
        "type": "object",
        "properties": {
          "event": {
            "type": "string",
            "enum": [
              "user_registered",
              "workspace_created",
              "board_created",
              "first_task_created",
              "first_drag",
              "invite_sent",
              "invite_accepted",
              "smtp_configured",
              "dashboard_viewed",
              "wau_board_view",
              "task_completed"
            ]
          },
          "count": {
            "type": "number"
          },
          "unit": {
            "type": "string",
            "description": "What `count` counts. `instance` steps are `1`/`0` and must never be subtracted across.",
            "enum": ["users", "instance"]
          },
          "window": {
            "type": "string",
            "enum": ["all-time", "rolling-week"]
          }
        },
        "required": ["event", "count", "unit", "window"]
      },
      "ActivationNorthStarSchema": {
        "type": "object",
        "properties": {
          "weeklyActiveTeamWorkspaces": {
            "type": "number"
          },
          "weeklyActiveWorkspaces": {
            "type": "number"
          },
          "teamWorkspaces": {
            "type": "number"
          },
          "windowDays": {
            "type": "number"
          }
        },
        "required": [
          "weeklyActiveTeamWorkspaces",
          "weeklyActiveWorkspaces",
          "teamWorkspaces",
          "windowDays"
        ]
      },
      "ActivationFunnelSchema": {
        "type": "object",
        "properties": {
          "generatedAt": {
            "type": "string",
            "description": "ISO 8601 UTC. Nothing is cached, so this is the moment of the request."
          },
          "steps": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActivationStepSchema"
            }
          },
          "northStar": {
            "$ref": "#/components/schemas/ActivationNorthStarSchema"
          }
        },
        "required": ["generatedAt", "steps", "northStar"]
      },
      "TaskAssigneeSchema": {
        "type": "object",
        "properties": {
          "userId": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "avatarUrl": {
            "type": "string",
            "nullable": true
          }
        },
        "required": ["userId", "name", "avatarUrl"]
      },
      "LabelSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "boardId": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "color": {
            "description": "A theme-resolved design-token slot name, never a raw hex value.",
            "enum": [
              "slot-1",
              "slot-2",
              "slot-3",
              "slot-4",
              "slot-5",
              "slot-6",
              "slot-7",
              "slot-8"
            ],
            "type": "string"
          }
        },
        "required": ["id", "boardId", "name", "color"]
      },
      "ChecklistSummarySchema": {
        "type": "object",
        "properties": {
          "total": {
            "type": "number"
          },
          "done": {
            "type": "number"
          }
        },
        "required": ["total", "done"]
      },
      "ChecklistItemSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "content": {
            "type": "string"
          },
          "isDone": {
            "type": "boolean"
          },
          "position": {
            "type": "number",
            "description": "Fractional index within the checklist."
          }
        },
        "required": ["id", "content", "isDone", "position"]
      },
      "ChecklistSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "position": {
            "type": "number",
            "description": "Fractional index within the task."
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ChecklistItemSchema"
            }
          }
        },
        "required": ["id", "title", "position", "items"]
      },
      "TaskSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "boardId": {
            "type": "string"
          },
          "columnId": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "description": {
            "type": "string",
            "nullable": true
          },
          "priority": {
            "enum": ["LOW", "MEDIUM", "HIGH", "URGENT"],
            "type": "string"
          },
          "position": {
            "type": "number",
            "description": "Fractional index within the column — a `Float`, never an integer and never contiguous.\n\nRewritten on every drag-and-drop. It decides where a card *appears*; the page boundary is\ndecided by `id`."
          },
          "dueDate": {
            "type": "string",
            "nullable": true,
            "description": "ISO 8601 UTC, or `null`. A date-only value is still a full timestamp at `T00:00:00.000Z`."
          },
          "estimatedMinutes": {
            "type": "number",
            "nullable": true,
            "description": "Integer minutes. Separate from `dueDate` — effort is not a deadline."
          },
          "createdById": {
            "type": "string"
          },
          "createdAt": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          },
          "assignees": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TaskAssigneeSchema"
            }
          },
          "labels": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LabelSchema"
            }
          },
          "checklistSummary": {
            "$ref": "#/components/schemas/ChecklistSummarySchema"
          },
          "checklists": {
            "nullable": true,
            "description": "Full checklists on a single-task read; `null` on list reads, which load only the summary.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ChecklistSchema"
            }
          },
          "attachmentCount": {
            "type": "number",
            "description": "A count, not a list: the card needs the number and the panel reads the list separately."
          }
        },
        "required": [
          "id",
          "boardId",
          "columnId",
          "title",
          "description",
          "priority",
          "position",
          "dueDate",
          "estimatedMinutes",
          "createdById",
          "createdAt",
          "updatedAt",
          "assignees",
          "labels",
          "checklistSummary",
          "checklists",
          "attachmentCount"
        ]
      },
      "TaskPageSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TaskSchema"
            }
          },
          "nextCursor": {
            "type": "string",
            "nullable": true,
            "description": "The `id` of the last item — pass as `?cursor=`. `null` on the last page."
          },
          "hasMore": {
            "type": "boolean"
          }
        },
        "required": ["items", "nextCursor", "hasMore"]
      },
      "CreateTaskDto": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "columnId": {
            "type": "string"
          },
          "description": {
            "type": "string",
            "nullable": true,
            "maxLength": 20000
          },
          "priority": {
            "type": "string",
            "description": "Omit to fall back to the column default (`Priority.MEDIUM`).",
            "enum": ["LOW", "MEDIUM", "HIGH", "URGENT"]
          },
          "dueDate": {
            "type": "string",
            "nullable": true
          },
          "estimatedMinutes": {
            "type": "number",
            "nullable": true,
            "description": "Kept separate from `dueDate` — an estimate is effort, a due date is a deadline.",
            "minimum": 0,
            "maximum": 525600
          },
          "afterTaskId": {
            "type": "string",
            "nullable": true,
            "description": "Insert after this task in the target column; omit to append."
          }
        },
        "required": ["title", "columnId"]
      },
      "UpdateTaskDto": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "description": {
            "type": "string",
            "nullable": true,
            "maxLength": 20000
          },
          "priority": {
            "type": "string",
            "enum": ["LOW", "MEDIUM", "HIGH", "URGENT"]
          },
          "dueDate": {
            "type": "string",
            "nullable": true
          },
          "estimatedMinutes": {
            "type": "number",
            "nullable": true,
            "minimum": 0,
            "maximum": 525600
          }
        }
      },
      "MoveTaskDto": {
        "type": "object",
        "properties": {
          "columnId": {
            "type": "string"
          },
          "beforeTaskId": {
            "type": "string",
            "nullable": true
          },
          "afterTaskId": {
            "type": "string",
            "nullable": true
          }
        },
        "required": ["columnId"]
      },
      "AddAssigneeDto": {
        "type": "object",
        "properties": {
          "userId": {
            "type": "string"
          }
        },
        "required": ["userId"]
      },
      "AddTaskLabelDto": {
        "type": "object",
        "properties": {
          "labelId": {
            "type": "string"
          }
        },
        "required": ["labelId"]
      },
      "CreateChecklistDto": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "maxLength": 255
          }
        },
        "required": ["title"]
      },
      "UpdateChecklistDto": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "maxLength": 255
          }
        },
        "required": ["title"]
      },
      "MoveChecklistDto": {
        "type": "object",
        "properties": {
          "afterId": {
            "type": "string",
            "description": "Checklist this one lands after; omitted means \"move to the front\"."
          }
        }
      },
      "CreateChecklistItemDto": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string",
            "maxLength": 1000
          }
        },
        "required": ["content"]
      },
      "UpdateChecklistItemDto": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string",
            "maxLength": 1000
          },
          "isDone": {
            "type": "boolean"
          }
        }
      },
      "MoveChecklistItemDto": {
        "type": "object",
        "properties": {
          "afterId": {
            "type": "string",
            "description": "Item this one lands after; omitted means \"move to the top of the checklist\"."
          }
        }
      },
      "NotificationSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "workspaceId": {
            "type": "string"
          },
          "userId": {
            "type": "string"
          },
          "type": {
            "type": "string",
            "description": "`NotificationType`."
          },
          "taskId": {
            "type": "string",
            "nullable": true
          },
          "activityId": {
            "type": "string",
            "nullable": true
          },
          "payload": {
            "type": "object",
            "additionalProperties": true
          },
          "readAt": {
            "type": "string",
            "nullable": true,
            "description": "ISO 8601 UTC when the recipient read it, or `null` while unread."
          },
          "createdAt": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "workspaceId",
          "userId",
          "type",
          "taskId",
          "activityId",
          "payload",
          "readAt",
          "createdAt"
        ]
      },
      "NotificationPageSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NotificationSchema"
            }
          },
          "nextCursor": {
            "type": "string",
            "nullable": true
          },
          "hasMore": {
            "type": "boolean"
          }
        },
        "required": ["items", "nextCursor", "hasMore"]
      },
      "NotificationUnreadCountSchema": {
        "type": "object",
        "properties": {
          "count": {
            "type": "number"
          }
        },
        "required": ["count"]
      },
      "MarkAllReadSchema": {
        "type": "object",
        "properties": {
          "updated": {
            "type": "number",
            "description": "Rows changed. `0` when everything was already read — not an error."
          }
        },
        "required": ["updated"]
      },
      "CreateLabelDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50
          },
          "color": {
            "type": "string",
            "enum": ["slot-1", "slot-2", "slot-3", "slot-4", "slot-5", "slot-6", "slot-7", "slot-8"]
          }
        },
        "required": ["name", "color"]
      },
      "UpdateLabelDto": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50
          },
          "color": {
            "type": "string",
            "enum": ["slot-1", "slot-2", "slot-3", "slot-4", "slot-5", "slot-6", "slot-7", "slot-8"]
          }
        }
      },
      "CommentSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "taskId": {
            "type": "string"
          },
          "userId": {
            "type": "string"
          },
          "body": {
            "type": "string"
          },
          "createdAt": {
            "type": "string"
          },
          "author": {
            "$ref": "#/components/schemas/AuthorSchema"
          }
        },
        "required": ["id", "taskId", "userId", "body", "createdAt", "author"]
      },
      "CommentPageSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommentSchema"
            }
          },
          "nextCursor": {
            "type": "string",
            "nullable": true
          },
          "hasMore": {
            "type": "boolean"
          }
        },
        "required": ["items", "nextCursor", "hasMore"]
      },
      "CreateCommentDto": {
        "type": "object",
        "properties": {
          "body": {
            "type": "string",
            "minLength": 1,
            "maxLength": 10000
          }
        },
        "required": ["body"]
      },
      "TrelloImportCountsSchema": {
        "type": "object",
        "properties": {
          "columns": {
            "type": "number"
          },
          "tasks": {
            "type": "number"
          },
          "labels": {
            "type": "number"
          },
          "checklists": {
            "type": "number"
          },
          "checklistItems": {
            "type": "number"
          },
          "attachments": {
            "type": "number"
          }
        },
        "required": ["columns", "tasks", "labels", "checklists", "checklistItems", "attachments"]
      },
      "TrelloImportSkipGroupSchema": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "string",
            "description": "Which part of the export this came from.",
            "enum": [
              "column",
              "label",
              "comment",
              "attachment",
              "checklist",
              "checklistItem",
              "list",
              "member",
              "card"
            ]
          },
          "reason": {
            "type": "string",
            "description": "Why it did not make it across.\n\n`defaulted` is in this list without being a skip: an imported column takes the default\ncategory and an unknown Trello colour falls back to `slot-1`. Both changed something the\nuser will see, and the question after an import is \"why does my board look different\".",
            "enum": [
              "outOfScope",
              "archived",
              "unmappable",
              "unsupportedScheme",
              "malformed",
              "defaulted"
            ]
          },
          "count": {
            "type": "number",
            "description": "The real number. Never capped."
          },
          "samples": {
            "description": "Up to 20 names, so the response scales with the number of *kinds* of problem.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["scope", "reason", "count", "samples"]
      },
      "TrelloImportReportSchema": {
        "type": "object",
        "properties": {
          "boardId": {
            "type": "string"
          },
          "boardName": {
            "type": "string"
          },
          "imported": {
            "$ref": "#/components/schemas/TrelloImportCountsSchema"
          },
          "skipped": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TrelloImportSkipGroupSchema"
            }
          }
        },
        "required": ["boardId", "boardName", "imported", "skipped"]
      },
      "DashboardCountByPrioritySchema": {
        "type": "object",
        "properties": {
          "priority": {
            "type": "string",
            "enum": ["LOW", "MEDIUM", "HIGH", "URGENT"]
          },
          "count": {
            "type": "number"
          }
        },
        "required": ["priority", "count"]
      },
      "DashboardCountByAssigneeSchema": {
        "type": "object",
        "properties": {
          "userId": {
            "type": "string",
            "nullable": true,
            "description": "`null` for the `Unassigned` and `Other` aggregate buckets."
          },
          "name": {
            "type": "string"
          },
          "count": {
            "type": "number"
          }
        },
        "required": ["userId", "name", "count"]
      },
      "DashboardCountByColumnSchema": {
        "type": "object",
        "properties": {
          "columnId": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "position": {
            "type": "number"
          },
          "count": {
            "type": "number"
          }
        },
        "required": ["columnId", "name", "position", "count"]
      },
      "DashboardThroughputDaySchema": {
        "type": "object",
        "properties": {
          "date": {
            "type": "string",
            "description": "`YYYY-MM-DD`, UTC — the one date in this API that is not a full timestamp."
          },
          "created": {
            "type": "number"
          },
          "completed": {
            "type": "number"
          }
        },
        "required": ["date", "created", "completed"]
      },
      "DashboardSummarySchema": {
        "type": "object",
        "properties": {
          "totalTasks": {
            "type": "number"
          },
          "overdueCount": {
            "type": "number"
          },
          "byPriority": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DashboardCountByPrioritySchema"
            }
          },
          "byAssignee": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DashboardCountByAssigneeSchema"
            }
          },
          "byColumn": {
            "nullable": true,
            "description": "Present only when `?boardId=` is set; `null` otherwise.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DashboardCountByColumnSchema"
            }
          },
          "throughput": {
            "description": "The last 14 UTC days.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DashboardThroughputDaySchema"
            }
          }
        },
        "required": [
          "totalTasks",
          "overdueCount",
          "byPriority",
          "byAssignee",
          "byColumn",
          "throughput"
        ]
      }
    }
  }
}
