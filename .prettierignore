node_modules
dist
.next
coverage
apps/api/src/generated
pnpm-lock.yaml
CHANGELOG.md
apps/web/next-env.d.ts
.claude/
# Playwright's own output: reports, traces, screenshots and the run manifest. Generated on
# every browser-suite run, gitignored, and not source.
e2e/test-results
e2e/playwright-report
e2e/blob-report
# A Trello export cut off mid-object — invalid JSON on purpose, so that the importer can be shown
# answering 400 instead of crashing on a half-finished download. `prettier --check` parses `.json`
# and would fail on it. Every other fixture in that directory is formatted normally; this is the
# one file whose brokenness is the point.
apps/api/test/fixtures/trello/edge-truncated.json
# Anonymised real Trello exports are the byte-for-byte output of
# scripts/anonymise-trello-export.mjs. Reformatting them would make the committed file differ
# from the one the script wrote, and the whole point of the directory is that nothing in it was
# touched by hand.
apps/api/test/fixtures/trello/real/*.json
